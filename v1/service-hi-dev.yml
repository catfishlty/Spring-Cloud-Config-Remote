server:
  port: 16699
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka1:16661/eureka/
    healthcheck:
      # 开启健康检查（需要spring-boot-starter-actuator依赖）
      enabled: true 
  instance:
    # 续约更新时间间隔（默认30秒）
    lease-renewal-interval-in-seconds: 10		
    # 续约到期时间（默认90秒）
    lease-expiration-duration-in-seconds: 30	