server:
  port: 16699
  # port: 16700
eureka:
  client:
    healthcheck:
      enabled: true 
  instance:
    # 续约更新时间间隔（默认30秒）
    lease-renewal-interval-in-seconds: 10		
    # 续约到期时间（默认90秒）
    lease-expiration-duration-in-seconds: 30	